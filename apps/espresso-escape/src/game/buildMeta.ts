/** Must match `app.json` expo.version / ios.buildNumber. TF must read newer than 1.0.5 / 12. */
export const APP_VERSION = "1.0.6";
export const IOS_BUILD = "18";
export const ANDROID_VERSION_CODE = 12;
export const BUILD_LABEL = `${APP_VERSION} · ${IOS_BUILD}`;
