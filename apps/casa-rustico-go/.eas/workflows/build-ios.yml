name: Build iOS

# Manual only — free tier iOS builds are limited. Trigger from expo.dev when ready.
on:
  workflow_dispatch: {}

jobs:
  build_ios:
    name: Build iOS (production)
    type: build
    params:
      platform: ios
      profile: production
