# Grok Build configuration

| Path | Role |
|------|------|
| `AGENTS.md` | Product contract |
| `.grok/config.toml` | Grok Build CLI + Cursor |
| `apps/hacienda` | Café PWA — web order-ahead / rewards |
| `apps/casa-rustico-go` | Shop — Expo base dir |
| `apps/espresso-escape` | Game — Expo base dir |

Open in Cursor. Point Expo (expo.dev) at the app folder, not the monorepo root. Point the Hacienda web app at `apps/hacienda`.
